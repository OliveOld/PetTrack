// classifier.h

#ifndef _CLASSIFIER_h
#define _CLASSIFIER_h

#if defined(ARDUINO) && ARDUINO >= 100
	#include "arduino.h"
#else
	#include "WProgram.h"
#endif

class classifier
{
public:
    void init();
};

#endif

